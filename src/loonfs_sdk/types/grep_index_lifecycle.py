# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .change_seq import ChangeSeq
from .checkpoint_id import CheckpointId


class GrepIndexLifecycle_Disabled(UniversalBaseModel):
    """
    Where a namespace's grep index is in its lifecycle.

    Each status contains only the fields valid for that lifecycle state.
    `Backfilling` reports its target and current position. `Active` reports
    how far the index has been built. Clients should treat a namespace as
    searchable only when the index is `Active`.
    """

    status: typing.Literal["disabled"] = "disabled"

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class GrepIndexLifecycle_Backfilling(UniversalBaseModel):
    """
    Where a namespace's grep index is in its lifecycle.

    Each status contains only the fields valid for that lifecycle state.
    `Backfilling` reports its target and current position. `Active` reports
    how far the index has been built. Clients should treat a namespace as
    searchable only when the index is `Active`.
    """

    status: typing.Literal["backfilling"] = "backfilling"
    checkpoint_id: CheckpointId
    cursor_inode_id: typing.Optional[str] = None
    target_seq: ChangeSeq

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class GrepIndexLifecycle_Active(UniversalBaseModel):
    """
    Where a namespace's grep index is in its lifecycle.

    Each status contains only the fields valid for that lifecycle state.
    `Backfilling` reports its target and current position. `Active` reports
    how far the index has been built. Clients should treat a namespace as
    searchable only when the index is `Active`.
    """

    status: typing.Literal["active"] = "active"
    built_through_seq: ChangeSeq
    next_event_index: typing.Optional[int] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


GrepIndexLifecycle = typing_extensions.Annotated[
    typing.Union[GrepIndexLifecycle_Disabled, GrepIndexLifecycle_Backfilling, GrepIndexLifecycle_Active],
    pydantic.Field(discriminator="status"),
]
