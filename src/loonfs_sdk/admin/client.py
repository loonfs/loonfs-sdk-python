# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.advance_retention_request import AdvanceRetentionRequest
from ..types.checkpoint import Checkpoint
from ..types.gc_request import GcRequest
from ..types.grep_gc_response import GrepGcResponse
from ..types.grep_index import GrepIndex
from ..types.list_checkpoints_response import ListCheckpointsResponse
from ..types.maintenance_step_response import MaintenanceStepResponse
from ..types.metadata_maintenance_request import MetadataMaintenanceRequest
from ..types.namespace_diagnostics import NamespaceDiagnostics
from ..types.release_checkpoint_response import ReleaseCheckpointResponse
from ..types.store_probe_request import StoreProbeRequest
from ..types.store_probe_response import StoreProbeResponse
from .raw_client import AsyncRawAdminClient, RawAdminClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AdminClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAdminClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAdminClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAdminClient
        """
        return self._raw_client

    def list_checkpoints(
        self,
        namespace_id: str,
        *,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListCheckpointsResponse:
        """
        Lists one page of active checkpoints in checkpoint-id order. Expired checkpoints remain visible until collection releases them. Released checkpoints are omitted. The cursor resumes a live listing and does not create a snapshot.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        limit : typing.Optional[int]
            Maximum page size

        cursor : typing.Optional[str]
            Opaque checkpoint-list page cursor

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCheckpointsResponse
            Active checkpoint objects

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.list_checkpoints(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.list_checkpoints(
            namespace_id, limit=limit, cursor=cursor, request_options=request_options
        )
        return _response.data

    def create_checkpoint(
        self,
        namespace_id: str,
        *,
        name: str,
        ttl_ms: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Checkpoint:
        """
        Creates a named, user-owned checkpoint record pinning the current namespace view. Every call mints a new record under a new id; the name is a label, not a key. The record is a garbage-collection root until it is released, so routine maintenance should flush the WAL instead. This is a maintenance/admin operation, not a file mutation.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        name : str
            Label recorded on the checkpoint record. A label, not a key: several
            records may carry the same name over different bases.

        ttl_ms : typing.Optional[int]
            Optional lifetime; the server computes the record's expiry from its
            own clock. Absent means the pin holds until explicitly released.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Checkpoint
            The created checkpoint

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.create_checkpoint(
            namespace_id="namespace_id",
            name="name",
        )
        """
        _response = self._raw_client.create_checkpoint(
            namespace_id, name=name, ttl_ms=ttl_ms, request_options=request_options
        )
        return _response.data

    def release_checkpoint(
        self, namespace_id: str, checkpoint_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ReleaseCheckpointResponse:
        """
        Releases a user-owned checkpoint pin by id. Idempotent: releasing an already-released or reaped record succeeds. The record is reaped by a later garbage-collection pass; its pinned data becomes collectable only on the pass after that.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        checkpoint_id : str
            Checkpoint id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ReleaseCheckpointResponse
            Checkpoint release accepted (including an already released or reaped checkpoint)

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.release_checkpoint(
            namespace_id="namespace_id",
            checkpoint_id="checkpoint_id",
        )
        """
        _response = self._raw_client.release_checkpoint(namespace_id, checkpoint_id, request_options=request_options)
        return _response.data

    def get_namespace_diagnostics(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> NamespaceDiagnostics:
        """
        Returns namespace state together with the current manifest and visible WAL tail.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        NamespaceDiagnostics
            Namespace diagnostics

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.get_namespace_diagnostics(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.get_namespace_diagnostics(namespace_id, request_options=request_options)
        return _response.data

    def get_grep_index(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Returns whether the namespace's grep index is `disabled`, `backfilling`, or `active`, including build progress when available. A namespace that has never enabled the index is `disabled`. This operation requires a deployment that maintains grep indexes and does not change the index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep index status and build progress

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.get_grep_index(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.get_grep_index(namespace_id, request_options=request_options)
        return _response.data

    def disable_grep_index(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Disables the namespace's grep root and clears its segment references with one durable compare-and-swap; index maintenance stops on its own once a step reads the disabled root. Explicit grep garbage collection later reclaims the segments. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root disabled or already disabled

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.disable_grep_index(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.disable_grep_index(namespace_id, request_options=request_options)
        return _response.data

    def enable_grep_index(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Enables the namespace's grep root and asks this deployment's maintenance runner for the backfill's first step. The response reports the lifecycle and bookkeeping read after the transition: a fresh enable is `backfilling` with the sequence its checkpoint captured, while an already-enabled namespace answers with its current status. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root enabled or already enabled

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.enable_grep_index(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.enable_grep_index(namespace_id, request_options=request_options)
        return _response.data

    def gc_grep_index(
        self,
        namespace_id: str,
        *,
        cursor: typing.Optional[str] = OMIT,
        max_objects: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GrepGcResponse:
        """
        Runs one explicit garbage-collection pass over only this namespace's grep-owned extension keyspace. A tombstoned or absent namespace has aged extension state reaped; no grep garbage collection runs implicitly. `max_objects` bounds the reads the pass spends and returns a `next_cursor` when keys remain; resuming re-reads liveness and the grep root, so a cursor only skips enumeration. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        cursor : typing.Optional[str]
            Opaque resume token returned as `next_cursor` by an earlier pass
            against the same namespace.

        max_objects : typing.Optional[int]
            Reads this pass may spend before returning with a `next_cursor`.
            Omit to take the same per-pass default the runtime's own collection
            takes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepGcResponse
            Namespace grep garbage collection completed

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.gc_grep_index(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.gc_grep_index(
            namespace_id, cursor=cursor, max_objects=max_objects, request_options=request_options
        )
        return _response.data

    def run_maintenance(
        self,
        namespace_id: str,
        *,
        gc: typing.Optional[GcRequest] = OMIT,
        metadata_maintenance: typing.Optional[MetadataMaintenanceRequest] = OMIT,
        retention: typing.Optional[AdvanceRetentionRequest] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MaintenanceStepResponse:
        """
        Runs one bounded maintenance step. Include `metadata_maintenance`, `retention`, or `gc` to select actions. Each selector is an options object, and an empty object uses server defaults. Actions run in that order, and only selected actions appear in the response. At least one action is required. A deleted namespace accepts only `gc`. GC processes up to 1024 candidates by default and returns a cursor when more work remains. A lost root update race is reported as an outcome.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        gc : typing.Optional[GcRequest]
            Run one bounded mark-and-sweep garbage-collection pass. Omit this
            field to skip garbage collection.

        metadata_maintenance : typing.Optional[MetadataMaintenanceRequest]
            Flush the visible WAL tail into metadata segments, then run one bounded
            reorganization step.

        retention : typing.Optional[AdvanceRetentionRequest]
            Advance the retention floor to the flushed manifest head. Include this
            field to select the action.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MaintenanceStepResponse
            Maintenance step completed

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.run_maintenance(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.run_maintenance(
            namespace_id,
            gc=gc,
            metadata_maintenance=metadata_maintenance,
            retention=retention,
            request_options=request_options,
        )
        return _response.data

    def probe_store(
        self, *, request: StoreProbeRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> StoreProbeResponse:
        """
        Proves the configured object store honours the create-if-absent, compare-and-swap, visibility, listing, and ranged-read semantics LoonFS depends on, and reports what it found check by check. Nothing runs this implicitly: a probe writes and deletes objects, all of them under a scratch prefix that is not a durable object family, and its last check deletes them and proves the prefix empty. A store that fails a check answers 200 with that check reported `failed` — the probe ran, and the answer is that the store is wrong. Optional capabilities a store declares it lacks answer `unsupported`, which is an answer rather than a fault. This route does not decide whether the deployment may serve presigned direct uploads: that trust comes from the endpoint allowlist, because a probe exercises the server's own request path and never a presigned capability handed to a client.

        Parameters
        ----------
        request : StoreProbeRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StoreProbeResponse
            Probe completed; per-check outcomes are in the body

        Examples
        --------
        from loonfs_sdk import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.probe_store(
            request={"key": "value"},
        )
        """
        _response = self._raw_client.probe_store(request=request, request_options=request_options)
        return _response.data


class AsyncAdminClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAdminClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAdminClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAdminClient
        """
        return self._raw_client

    async def list_checkpoints(
        self,
        namespace_id: str,
        *,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListCheckpointsResponse:
        """
        Lists one page of active checkpoints in checkpoint-id order. Expired checkpoints remain visible until collection releases them. Released checkpoints are omitted. The cursor resumes a live listing and does not create a snapshot.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        limit : typing.Optional[int]
            Maximum page size

        cursor : typing.Optional[str]
            Opaque checkpoint-list page cursor

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCheckpointsResponse
            Active checkpoint objects

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.list_checkpoints(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_checkpoints(
            namespace_id, limit=limit, cursor=cursor, request_options=request_options
        )
        return _response.data

    async def create_checkpoint(
        self,
        namespace_id: str,
        *,
        name: str,
        ttl_ms: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Checkpoint:
        """
        Creates a named, user-owned checkpoint record pinning the current namespace view. Every call mints a new record under a new id; the name is a label, not a key. The record is a garbage-collection root until it is released, so routine maintenance should flush the WAL instead. This is a maintenance/admin operation, not a file mutation.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        name : str
            Label recorded on the checkpoint record. A label, not a key: several
            records may carry the same name over different bases.

        ttl_ms : typing.Optional[int]
            Optional lifetime; the server computes the record's expiry from its
            own clock. Absent means the pin holds until explicitly released.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Checkpoint
            The created checkpoint

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.create_checkpoint(
                namespace_id="namespace_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_checkpoint(
            namespace_id, name=name, ttl_ms=ttl_ms, request_options=request_options
        )
        return _response.data

    async def release_checkpoint(
        self, namespace_id: str, checkpoint_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ReleaseCheckpointResponse:
        """
        Releases a user-owned checkpoint pin by id. Idempotent: releasing an already-released or reaped record succeeds. The record is reaped by a later garbage-collection pass; its pinned data becomes collectable only on the pass after that.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        checkpoint_id : str
            Checkpoint id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ReleaseCheckpointResponse
            Checkpoint release accepted (including an already released or reaped checkpoint)

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.release_checkpoint(
                namespace_id="namespace_id",
                checkpoint_id="checkpoint_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.release_checkpoint(
            namespace_id, checkpoint_id, request_options=request_options
        )
        return _response.data

    async def get_namespace_diagnostics(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> NamespaceDiagnostics:
        """
        Returns namespace state together with the current manifest and visible WAL tail.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        NamespaceDiagnostics
            Namespace diagnostics

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.get_namespace_diagnostics(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_namespace_diagnostics(namespace_id, request_options=request_options)
        return _response.data

    async def get_grep_index(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Returns whether the namespace's grep index is `disabled`, `backfilling`, or `active`, including build progress when available. A namespace that has never enabled the index is `disabled`. This operation requires a deployment that maintains grep indexes and does not change the index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep index status and build progress

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.get_grep_index(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_grep_index(namespace_id, request_options=request_options)
        return _response.data

    async def disable_grep_index(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Disables the namespace's grep root and clears its segment references with one durable compare-and-swap; index maintenance stops on its own once a step reads the disabled root. Explicit grep garbage collection later reclaims the segments. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root disabled or already disabled

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.disable_grep_index(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.disable_grep_index(namespace_id, request_options=request_options)
        return _response.data

    async def enable_grep_index(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Enables the namespace's grep root and asks this deployment's maintenance runner for the backfill's first step. The response reports the lifecycle and bookkeeping read after the transition: a fresh enable is `backfilling` with the sequence its checkpoint captured, while an already-enabled namespace answers with its current status. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root enabled or already enabled

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.enable_grep_index(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.enable_grep_index(namespace_id, request_options=request_options)
        return _response.data

    async def gc_grep_index(
        self,
        namespace_id: str,
        *,
        cursor: typing.Optional[str] = OMIT,
        max_objects: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GrepGcResponse:
        """
        Runs one explicit garbage-collection pass over only this namespace's grep-owned extension keyspace. A tombstoned or absent namespace has aged extension state reaped; no grep garbage collection runs implicitly. `max_objects` bounds the reads the pass spends and returns a `next_cursor` when keys remain; resuming re-reads liveness and the grep root, so a cursor only skips enumeration. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        cursor : typing.Optional[str]
            Opaque resume token returned as `next_cursor` by an earlier pass
            against the same namespace.

        max_objects : typing.Optional[int]
            Reads this pass may spend before returning with a `next_cursor`.
            Omit to take the same per-pass default the runtime's own collection
            takes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepGcResponse
            Namespace grep garbage collection completed

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.gc_grep_index(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.gc_grep_index(
            namespace_id, cursor=cursor, max_objects=max_objects, request_options=request_options
        )
        return _response.data

    async def run_maintenance(
        self,
        namespace_id: str,
        *,
        gc: typing.Optional[GcRequest] = OMIT,
        metadata_maintenance: typing.Optional[MetadataMaintenanceRequest] = OMIT,
        retention: typing.Optional[AdvanceRetentionRequest] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MaintenanceStepResponse:
        """
        Runs one bounded maintenance step. Include `metadata_maintenance`, `retention`, or `gc` to select actions. Each selector is an options object, and an empty object uses server defaults. Actions run in that order, and only selected actions appear in the response. At least one action is required. A deleted namespace accepts only `gc`. GC processes up to 1024 candidates by default and returns a cursor when more work remains. A lost root update race is reported as an outcome.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        gc : typing.Optional[GcRequest]
            Run one bounded mark-and-sweep garbage-collection pass. Omit this
            field to skip garbage collection.

        metadata_maintenance : typing.Optional[MetadataMaintenanceRequest]
            Flush the visible WAL tail into metadata segments, then run one bounded
            reorganization step.

        retention : typing.Optional[AdvanceRetentionRequest]
            Advance the retention floor to the flushed manifest head. Include this
            field to select the action.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MaintenanceStepResponse
            Maintenance step completed

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.run_maintenance(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.run_maintenance(
            namespace_id,
            gc=gc,
            metadata_maintenance=metadata_maintenance,
            retention=retention,
            request_options=request_options,
        )
        return _response.data

    async def probe_store(
        self, *, request: StoreProbeRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> StoreProbeResponse:
        """
        Proves the configured object store honours the create-if-absent, compare-and-swap, visibility, listing, and ranged-read semantics LoonFS depends on, and reports what it found check by check. Nothing runs this implicitly: a probe writes and deletes objects, all of them under a scratch prefix that is not a durable object family, and its last check deletes them and proves the prefix empty. A store that fails a check answers 200 with that check reported `failed` — the probe ran, and the answer is that the store is wrong. Optional capabilities a store declares it lacks answer `unsupported`, which is an answer rather than a fault. This route does not decide whether the deployment may serve presigned direct uploads: that trust comes from the endpoint allowlist, because a probe exercises the server's own request path and never a presigned capability handed to a client.

        Parameters
        ----------
        request : StoreProbeRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StoreProbeResponse
            Probe completed; per-check outcomes are in the body

        Examples
        --------
        import asyncio

        from loonfs_sdk import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.probe_store(
                request={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.probe_store(request=request, request_options=request_options)
        return _response.data
