# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ...types.grep_gc_response import GrepGcResponse
from ...types.grep_index import GrepIndex
from .raw_client import AsyncRawGrepIndexClient, RawGrepIndexClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class GrepIndexClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawGrepIndexClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawGrepIndexClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawGrepIndexClient
        """
        return self._raw_client

    def retrieve(self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GrepIndex:
        """
        Returns whether the namespace's grep index is `disabled`, `backfilling`, or `active`, including build progress when available. A namespace that has never enabled the index is `disabled`. This operation requires a deployment that maintains grep indexes and does not change the index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep index status and build progress

        Examples
        --------
        from loonfs.server import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.grep_index.retrieve(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.retrieve(namespace_id, request_options=request_options)
        return _response.data

    def disable(self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GrepIndex:
        """
        Disables the namespace's grep root and clears its segment references with one durable compare-and-swap; index maintenance stops on its own once a step reads the disabled root. Explicit grep garbage collection later reclaims the segments. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root disabled or already disabled

        Examples
        --------
        from loonfs.server import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.grep_index.disable(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.disable(namespace_id, request_options=request_options)
        return _response.data

    def enable(self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GrepIndex:
        """
        Enables the namespace's grep root and asks this deployment's maintenance runner for the backfill's first step. The response reports the lifecycle and bookkeeping read after the transition: a fresh enable is `backfilling` with the sequence its checkpoint captured, while an already-enabled namespace answers with its current status. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root enabled or already enabled

        Examples
        --------
        from loonfs.server import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.grep_index.enable(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.enable(namespace_id, request_options=request_options)
        return _response.data

    def gc(
        self,
        namespace_id: str,
        *,
        cursor: typing.Optional[str] = OMIT,
        max_objects: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GrepGcResponse:
        """
        Runs one explicit garbage-collection pass over only this namespace's grep-owned extension keyspace. A tombstoned or absent namespace has aged extension state reaped; no grep garbage collection runs implicitly. `max_objects` bounds the reads the pass spends and returns a `next_cursor` when keys remain; resuming re-reads liveness and the grep root, so a cursor only skips enumeration. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        cursor : typing.Optional[str]
            Opaque resume token returned as `next_cursor` by an earlier pass
            against the same namespace.

        max_objects : typing.Optional[int]
            Reads this pass may spend before returning with a `next_cursor`.
            Omit to take the same per-pass default the runtime's own collection
            takes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepGcResponse
            Namespace grep garbage collection completed

        Examples
        --------
        from loonfs.server import LoonFS

        client = LoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )
        client.admin.grep_index.gc(
            namespace_id="namespace_id",
        )
        """
        _response = self._raw_client.gc(
            namespace_id, cursor=cursor, max_objects=max_objects, request_options=request_options
        )
        return _response.data


class AsyncGrepIndexClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawGrepIndexClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawGrepIndexClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawGrepIndexClient
        """
        return self._raw_client

    async def retrieve(
        self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GrepIndex:
        """
        Returns whether the namespace's grep index is `disabled`, `backfilling`, or `active`, including build progress when available. A namespace that has never enabled the index is `disabled`. This operation requires a deployment that maintains grep indexes and does not change the index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep index status and build progress

        Examples
        --------
        import asyncio

        from loonfs.server import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.grep_index.retrieve(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(namespace_id, request_options=request_options)
        return _response.data

    async def disable(self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GrepIndex:
        """
        Disables the namespace's grep root and clears its segment references with one durable compare-and-swap; index maintenance stops on its own once a step reads the disabled root. Explicit grep garbage collection later reclaims the segments. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root disabled or already disabled

        Examples
        --------
        import asyncio

        from loonfs.server import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.grep_index.disable(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.disable(namespace_id, request_options=request_options)
        return _response.data

    async def enable(self, namespace_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GrepIndex:
        """
        Enables the namespace's grep root and asks this deployment's maintenance runner for the backfill's first step. The response reports the lifecycle and bookkeeping read after the transition: a fresh enable is `backfilling` with the sequence its checkpoint captured, while an already-enabled namespace answers with its current status. Idempotent. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepIndex
            Grep root enabled or already enabled

        Examples
        --------
        import asyncio

        from loonfs.server import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.grep_index.enable(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.enable(namespace_id, request_options=request_options)
        return _response.data

    async def gc(
        self,
        namespace_id: str,
        *,
        cursor: typing.Optional[str] = OMIT,
        max_objects: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GrepGcResponse:
        """
        Runs one explicit garbage-collection pass over only this namespace's grep-owned extension keyspace. A tombstoned or absent namespace has aged extension state reaped; no grep garbage collection runs implicitly. `max_objects` bounds the reads the pass spends and returns a `next_cursor` when keys remain; resuming re-reads liveness and the grep root, so a cursor only skips enumeration. Requires this deployment to maintain the grep index.

        Parameters
        ----------
        namespace_id : str
            Namespace id

        cursor : typing.Optional[str]
            Opaque resume token returned as `next_cursor` by an earlier pass
            against the same namespace.

        max_objects : typing.Optional[int]
            Reads this pass may spend before returning with a `next_cursor`.
            Omit to take the same per-pass default the runtime's own collection
            takes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GrepGcResponse
            Namespace grep garbage collection completed

        Examples
        --------
        import asyncio

        from loonfs.server import AsyncLoonFS

        client = AsyncLoonFS(
            token="YOUR_TOKEN",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.admin.grep_index.gc(
                namespace_id="namespace_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.gc(
            namespace_id, cursor=cursor, max_objects=max_objects, request_options=request_options
        )
        return _response.data
